import {connect} from 'dva';
import React from 'react';
import Page from '../../components/body/body';

const RoleList = ({dispatch}) => {
  return (
    <Page>
      <div>123</div>
    </Page>
  )
}

export default connect()(RoleList);